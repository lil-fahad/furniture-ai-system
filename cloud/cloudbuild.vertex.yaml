steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - --file
      - cloud/Dockerfile.vertex
      - --tag
      - ${_IMAGE_URI}
      - .
images:
  - ${_IMAGE_URI}
options:
  logging: CLOUD_LOGGING_ONLY
