steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - --file
      - cloud/Dockerfile.vertex
      - --tag
      - ${_IMAGE_URI}
      - .
images:
  - ${_IMAGE_URI}
options:
  machineType: E2_HIGHCPU_8
  logging: CLOUD_LOGGING_ONLY
